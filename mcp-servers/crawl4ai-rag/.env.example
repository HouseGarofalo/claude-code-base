# =============================================================================
# Crawl4AI MCP Server Configuration
# =============================================================================
# Copy this file to .env and fill in your values
# Credentials are also loaded from ~/.claude/.env (global fallback)
# =============================================================================

# -----------------------------------------------------------------------------
# Server Configuration
# -----------------------------------------------------------------------------

# Transport mode: stdio (default) or sse
TRANSPORT=stdio

# Port for SSE transport (only used when TRANSPORT=sse)
SSE_PORT=8000

# -----------------------------------------------------------------------------
# Browser Configuration
# -----------------------------------------------------------------------------

# Run browser in headless mode (recommended for server environments)
HEADLESS=true

# Browser type to use
# Options: chromium (default), firefox, webkit
BROWSER_TYPE=chromium

# User agent string (optional, uses browser default if not set)
# USER_AGENT=Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36

# Viewport dimensions (optional)
# VIEWPORT_WIDTH=1920
# VIEWPORT_HEIGHT=1080

# -----------------------------------------------------------------------------
# Rate Limiting
# -----------------------------------------------------------------------------

# Mean delay between requests in seconds (helps avoid rate limiting)
MEAN_DELAY=0.5

# Maximum concurrent crawl operations
MAX_CONCURRENT=5

# Request timeout in seconds
# REQUEST_TIMEOUT=30

# -----------------------------------------------------------------------------
# Azure OpenAI (for embeddings and LLM extraction)
# -----------------------------------------------------------------------------
# Get these from Azure Portal > Azure OpenAI > Keys and Endpoint
# Or from Azure AI Studio > Deployments

AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/
AZURE_OPENAI_API_KEY=your-azure-openai-api-key
AZURE_OPENAI_API_VERSION=2024-12-01-preview

# The name of your embedding model deployment in Azure
# Common values: text-embedding-3-small, text-embedding-ada-002
# Check Azure AI Studio for your exact deployment name
AZURE_EMBEDDING_DEPLOYMENT=text-embedding-3-small

# -----------------------------------------------------------------------------
# Supabase (Vector RAG - pgvector)
# -----------------------------------------------------------------------------
# Get these from Supabase Dashboard > Settings > API

SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Alternative key names (server checks both)
# SUPABASE_SERVICE_KEY=...

# -----------------------------------------------------------------------------
# Neo4j (Graph RAG - Knowledge Graph)
# -----------------------------------------------------------------------------
# For local Neo4j Desktop or Docker: bolt://localhost:7687
# For Neo4j Aura cloud: neo4j+s://xxxxx.databases.neo4j.io

NEO4J_URI=bolt://localhost:7687
NEO4J_USERNAME=neo4j
NEO4J_PASSWORD=your-neo4j-password

# Alternative environment variable names (server checks both)
# NEO4J_ENDPONT1=bolt://localhost:7687
# NEO4J_USER=neo4j

# -----------------------------------------------------------------------------
# Logging Configuration (Optional)
# -----------------------------------------------------------------------------

# Log level (DEBUG, INFO, WARNING, ERROR)
# LOG_LEVEL=INFO

# Log format (optional)
# LOG_FORMAT=%(asctime)s - %(name)s - %(levelname)s - %(message)s

# -----------------------------------------------------------------------------
# Proxy Configuration (Optional)
# -----------------------------------------------------------------------------

# HTTP proxy URL
# HTTP_PROXY=http://proxy.example.com:8080

# HTTPS proxy URL
# HTTPS_PROXY=https://proxy.example.com:8080

# Proxy authentication (if required)
# PROXY_USERNAME=
# PROXY_PASSWORD=

# =============================================================================
# CREDENTIAL LOADING ORDER
# =============================================================================
# 1. Local .env file (this file) - highest priority
# 2. Global ~/.claude/.env - fallback for shared credentials
#
# If you have credentials in ~/.claude/.env, you don't need to duplicate
# them here. The server automatically loads from both locations.
# =============================================================================
